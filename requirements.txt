# --- Core (required) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
anthropic>=1.0          # the "brain" (Claude); pulls in its own HTTP stack

# --- Actions (optional; the app degrades gracefully if any are missing) ---
# If a wheel is unavailable for your Python version, install what you can:
#   pip install -r requirements.txt || pip install fastapi uvicorn python-dotenv anthropic
spotipy>=2.24           # Spotify playback control
pycaw>=20240210         # Windows system volume
comtypes>=1.4           # required by pycaw
screen-brightness-control>=0.24   # screen brightness
